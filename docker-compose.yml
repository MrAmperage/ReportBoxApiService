version: '3.8'

services:
  krakend:
    image: krakend:latest
    container_name: ReportBox_Api
    ports:
      - "8080:8080"
    volumes:
      - ./krakend.json:/etc/krakend/krakend.json
    command: run -c /etc/krakend/krakend.json
    restart: unless-stopped